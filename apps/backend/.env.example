# 서버 설정
PORT=3001
NODE_ENV=development

# 저장소 설정 (우선순위 순)
# 1순위: 외장 디스크 경로
STORAGE_PATH=/Volumes/ExternalHDD/SaveMyData
# STORAGE_PATH=/mnt/external-hdd/SaveMyData  # Linux
# STORAGE_PATH=E:\SaveMyData                 # Windows

# 2순위: 네트워크 드라이브 (NAS)
FALLBACK_STORAGE_PATH=/Volumes/NAS/BackupStorage
# FALLBACK_STORAGE_PATH=/mnt/nas/backup     # Linux  
# FALLBACK_STORAGE_PATH=\\NAS\BackupShare   # Windows

# 3순위: 로컬 프로젝트 경로 (자동 설정됨)
# LOCAL_STORAGE_PATH=../../storage

# 저장소 헬스체크 간격 (밀리초)
STORAGE_HEALTH_CHECK_INTERVAL=30000

# 파일 업로드 제한
MAX_FILE_SIZE=524288000  # 500MB
MAX_FILES_PER_REQUEST=10

# 로그 설정
LOG_LEVEL=info